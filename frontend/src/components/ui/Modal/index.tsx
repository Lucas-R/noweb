import Wrapper from "./wrapper";
import Header from "./Header";
import Body from "./Body";
import Footer from "./Footer";

export const Modal = {
    wrapper: Wrapper,
    header: Header,
    body: Body,
    footer: Footer,
}